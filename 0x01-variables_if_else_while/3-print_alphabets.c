#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 *  main - Alphabetic
 *
 *  Description: Uses for
 *  Return: void
 */

int main(void)
{
	char ch, ah;
	for (ch = 97; ch <= 122; ch++)
		putchar(ch);

	for (ah = 65; ah <= 90; ah++)
		putchar(ah);

	putchar('\n');
	return (0);
}
