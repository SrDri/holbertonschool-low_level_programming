#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 *  main - Alphabetic
 *
 *  Description: Uses for
 *  Return: return 0
 */

int main(void)
{
	int ch;

	for (ch = 'a'; ch <= 'z'; ch++)
		putchar(ch);

	putchar('\n');

	return (0);
}
